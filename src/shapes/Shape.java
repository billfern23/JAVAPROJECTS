package shapes;
// Shape class, this the interface or bases for all the other shapes to inherit from except parallelogram.
@FunctionalInterface
public interface Shape {
	 double calculatePerimeter();
	 
}
